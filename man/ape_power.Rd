% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{ape_power}
\alias{ape_power}
\title{Simulated power for an APE/AIE claim at a given sample size}
\usage{
ape_power(
  dgp,
  n,
  claim = c("minimum", "detect", "equivalence"),
  sesoi = NULL,
  conf = 0.95,
  nsim = 1000,
  seed = NULL,
  se = c("model", "robust")
)
}
\arguments{
\item{dgp}{A \code{powerape_dgp} after \code{\link[=set_ape]{set_ape()}} or \code{\link[=set_aie]{set_aie()}}.}

\item{n}{Total sample size of the simulated study.}

\item{claim}{\code{"minimum"} (default), \code{"detect"}, or \code{"equivalence"}.}

\item{sesoi}{Smallest effect size of interest, in APE units. Required for
\code{"minimum"} and \code{"equivalence"}; optional for \code{"detect"} (if supplied,
the outcome table is still broken out against it).}

\item{conf}{CI level (default 0.95).}

\item{nsim}{Number of simulation replications.}

\item{seed}{Optional seed (the caller's RNG state is preserved).}

\item{se}{Standard errors for the exogenous cross-sectional routes:
\code{"model"} (default, expected-information ML) or \code{"robust"}
(heteroskedasticity-robust HC0 sandwich, as in the sandwich package).
Panel designs always use unit-clustered SEs and IV designs the
stacked method-of-moments robust sandwich; \code{se} is ignored there.}
}
\value{
A \code{powerape_power} object: power, Monte Carlo standard error,
outcome distribution, the failed-fit count (failures count against
power, conservatively), and the embedded DGP spec for reproducibility
and \code{\link[=power_statement]{power_statement()}}.
}
\description{
Simulates studies from the pinned DGP, fits the index model by maximum
likelihood, computes the APE (or, for moderated DGPs, the AIE) with a
delta-method Wald CI, and evaluates the requested confidence-interval
claim (Riesthuis, 2024): \code{"minimum"} (CI lower bound above the SESOI --
the default and the package's point), \code{"detect"} (CI excludes 0,
directional), or \code{"equivalence"} (CI within +/- SESOI). Also reports the
full outcome distribution.
}
\examples{
\donttest{
d <- ape_dgp(focal = pa_var("treat", "binary", p = 0.5), baseline = 0.30)
d <- set_ape(d, target = 0.10)
ape_power(d, n = 800, claim = "minimum", sesoi = 0.03, nsim = 500, seed = 1)
}
}
