% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical.R
\name{ape_dgp_empirical}
\alias{ape_dgp_empirical}
\title{Specify a DGP with covariates resampled from pilot data}
\usage{
ape_dgp_empirical(
  model = c("probit", "logit"),
  data,
  focal,
  baseline,
  signal = 0,
  gamma = NULL,
  seed_int = 20260812L
)
}
\arguments{
\item{model}{\code{"probit"} (default) or \code{"logit"}.}

\item{data}{Data frame or matrix of numeric columns, no missing values.
Dummy-encode factors first.}

\item{focal}{Column name in \code{data}, or a \code{\link[=pa_var]{pa_var()}}.}

\item{baseline}{Baseline outcome rate in (0, 1): the average \code{P(Y = 1)}
with the focal at its reference value (0 for binary, the mean for
continuous), covariates at their empirical distribution.}

\item{signal}{Nuisance-signal strength (latent pseudo-R-squared of the
nuisance index), as in \code{\link[=ape_dgp]{ape_dgp()}}; equal weights on the standardized
scale, scaled on the empirical rows. Ignored when \code{gamma} is supplied.}

\item{gamma}{Optional explicit nuisance coefficients, one per covariate
column.}

\item{seed_int}{Seed for the deterministic draw used only when a
continuous \code{pa_var} focal is combined with empirical covariates.}
}
\value{
An object of class \code{powerape_dgp} (route \code{"empirical"}).
}
\description{
Instead of parametric marginals and a copula, the covariate distribution
is the empirical joint distribution of a pilot (or register) dataset:
simulation resamples its rows with replacement, and all calibration
integrals are exact averages over the rows. This preserves the covariate
dependence structure for free and is the recommended route when data
exist (DESIGN.md section 3.1).
}
\details{
The focal variable comes in two flavors:
\itemize{
\item a \strong{column name} in \code{data}: the focal variable is resampled jointly
with the covariates, preserving its empirical dependence with them
(observational mental model). Type is inferred (binary if the column is
0/1, continuous otherwise; a continuous focal's reference value is its
mean).
\item a \strong{\code{\link[=pa_var]{pa_var()}}}: the focal variable is drawn independently of the
resampled rows (randomized-treatment mental model); all of \code{data} is
treated as covariates.
}
}
\examples{
pilot <- data.frame(treat = rbinom(600, 1, 0.5),
                    age = rnorm(600, 45, 12))
d <- ape_dgp_empirical(data = pilot, focal = "treat",
                       baseline = 0.30, signal = 0.15)
d
}
