% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mde.R
\name{ape_mde}
\alias{ape_mde}
\title{Minimum detectable APE/AIE at a given sample size}
\usage{
ape_mde(
  dgp,
  n,
  power = 0.8,
  claim = c("detect", "minimum", "equivalence"),
  sesoi = NULL,
  conf = 0.95,
  nsim = 1000,
  seed = NULL,
  main_focal = NULL,
  main_moderator = NULL,
  max_iter = 6,
  confirm = TRUE,
  nsim_confirm = 4 * nsim,
  se = c("model", "robust")
)
}
\arguments{
\item{dgp}{A \code{powerape_dgp} after \code{\link[=set_ape]{set_ape()}} or \code{\link[=set_aie]{set_aie()}}.}

\item{n}{Sample size of the planned study (units for panel designs).}

\item{power}{Target power for the claim (default 0.80).}

\item{claim}{\code{"minimum"} (default), \code{"detect"}, or \code{"equivalence"}.}

\item{sesoi}{Smallest effect size of interest, in APE units. Required for
\code{"minimum"} and \code{"equivalence"}; optional for \code{"detect"} (if supplied,
the outcome table is still broken out against it).}

\item{conf}{CI level (default 0.95).}

\item{nsim}{Number of simulation replications.}

\item{seed}{Optional seed (the caller's RNG state is preserved).}

\item{main_focal, main_moderator}{Main-effect anchors for AIE designs;
defaults to the values stored by a previous \code{\link[=set_aie]{set_aie()}} call.}

\item{max_iter}{Maximum search refinements.}

\item{confirm, nsim_confirm}{Confirmation stage as in \code{\link[=ape_n]{ape_n()}}.}

\item{se}{Standard errors for the exogenous cross-sectional routes:
\code{"model"} (default, expected-information ML) or \code{"robust"}
(heteroskedasticity-robust HC0 sandwich, as in the sandwich package).
Panel designs always use unit-clustered SEs and IV designs the
stacked method-of-moments robust sandwich; \code{se} is ignored there.}
}
\value{
A \code{powerape_mde} object: \code{mde} (the minimum detectable effect,
or for equivalence the smallest establishable margin), the confirmed
\code{power} and \code{mcse} at that effect, the search \code{history}, and the DGP
re-pinned at the answer (so the object feeds \code{\link[=power_statement]{power_statement()}}).
}
\description{
The inverse of \code{\link[=ape_n]{ape_n()}}: the sample size is fixed -- an archive with
so many firms, a panel with so many units and waves -- and the
question is the smallest effect the design can reliably conclude.
For \code{claim = "detect"} and \code{claim = "minimum"} the function searches
over the assumed true effect, re-running the APE/AIE inversion at
every candidate, until simulated power at \code{n} hits the goal; for
\code{claim = "equivalence"} the target is held at the DGP's pinned value
(typically 0) and the search is over the margin instead, returning
the tightest equivalence bounds the design can expect to establish.
}
\details{
By default the answer is verified the way \code{\link[=ape_n]{ape_n()}} verifies its n: a
high-precision confirmation stage re-measures power at the candidate
and pushes the effect upward (never downward) if it falls short, so
the reported MDE errs on the conservative side.

For a DGP with a moderator the searched effect is the AIE; the two
conditional-at-reference main-effect anchors are taken from the
pinned DGP (or passed via \code{main_focal}/\code{main_moderator}) and held
fixed across candidates.
}
\examples{
\donttest{
d <- ape_dgp(focal = pa_var("treat", "binary", p = 0.5), baseline = 0.30)
ape_mde(d, n = 712, claim = "detect", nsim = 600, seed = 1)
}
}
