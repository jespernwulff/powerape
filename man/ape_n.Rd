% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve.R
\name{ape_n}
\alias{ape_n}
\title{Required sample size for a target power}
\usage{
ape_n(
  dgp,
  power = 0.9,
  claim = c("minimum", "detect", "equivalence"),
  sesoi = NULL,
  conf = 0.95,
  nsim = 1500,
  seed = NULL,
  n_range = c(30, 2e+06),
  max_iter = 5,
  confirm = TRUE,
  nsim_confirm = 4 * nsim,
  se = c("model", "robust")
)
}
\arguments{
\item{dgp}{A \code{powerape_dgp} after \code{\link[=set_ape]{set_ape()}} or \code{\link[=set_aie]{set_aie()}}.}

\item{power}{Target power (default 0.90).}

\item{claim}{\code{"minimum"} (default), \code{"detect"}, or \code{"equivalence"}.}

\item{sesoi}{Smallest effect size of interest, in APE units. Required for
\code{"minimum"} and \code{"equivalence"}; optional for \code{"detect"} (if supplied,
the outcome table is still broken out against it).}

\item{conf}{CI level (default 0.95).}

\item{nsim}{Replications per search step.}

\item{seed}{Optional seed (the caller's RNG state is preserved).}

\item{n_range}{Admissible range for the answer; hitting the ceiling warns.}

\item{max_iter}{Maximum search iterations.}

\item{confirm}{Run the high-precision confirmation stage (default TRUE).
Set to FALSE for speed in exploratory loops; the returned power then
carries search-stage precision only.}

\item{nsim_confirm}{Replications per confirmation round (default
\code{4 * nsim}).}

\item{se}{Standard errors for the exogenous cross-sectional routes:
\code{"model"} (default, expected-information ML) or \code{"robust"}
(heteroskedasticity-robust HC0 sandwich, as in the sandwich package).
Panel designs always use unit-clustered SEs and IV designs the
stacked method-of-moments robust sandwich; \code{se} is ignored there.}
}
\value{
A \code{powerape_n} object: the required \code{n}, the confirmed (or, with
\code{confirm = FALSE}, search-stage) power and MCSE at that \code{n}, the
search history (\code{stage} column distinguishes search and confirmation
rounds), and the embedded DGP.
}
\description{
Search plus confirmation. A pilot run estimates the SE scale
(SE ~ c/sqrt(n)), a normal-approximation formula proposes n, and
simulation at the proposal verifies and adjusts until the estimated power
is within Monte Carlo precision of the goal. By default a
\strong{confirmation stage} then re-measures the candidate at \code{nsim_confirm}
replications and accepts only if the confirmed power is no more than
\code{max(0.005, 1.5 * MCSE)} below the goal, pushing n upward otherwise (up
to three rounds). The confirmation never trims n on overshoot -- slight
overshoot is the conservative direction for sample-size planning. The
returned power and MCSE come from the confirmation run. Works for APE
and AIE designs alike.
}
\examples{
\donttest{
d <- ape_dgp(focal = pa_var("treat", "binary", p = 0.5), baseline = 0.30)
d <- set_ape(d, target = 0.10)
ape_n(d, power = 0.8, claim = "detect", nsim = 600, seed = 1)
}
}
