% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robust.R
\name{ape_robust}
\alias{ape_robust}
\title{Robustness sweep over contextual assumptions (worst-case power and n_max)}
\usage{
ape_robust(
  dgp,
  n,
  claim = c("minimum", "detect", "equivalence"),
  sesoi = NULL,
  conf = 0.95,
  nsim = 800,
  seed = NULL,
  vary,
  pin = c("ape", "coefficients"),
  mode = c("power", "mde"),
  power = 0.8,
  grid_points = 3,
  nmax = TRUE,
  nmax_power = 0.9
)
}
\arguments{
\item{dgp}{A \code{powerape_dgp} after \code{\link[=set_ape]{set_ape()}} or \code{\link[=set_aie]{set_aie()}}.}

\item{n}{Total sample size of the simulated study.}

\item{claim}{\code{"minimum"} (default), \code{"detect"}, or \code{"equivalence"}.}

\item{sesoi}{Smallest effect size of interest, in APE units. Required for
\code{"minimum"} and \code{"equivalence"}; optional for \code{"detect"} (if supplied,
the outcome table is still broken out against it).}

\item{conf}{CI level (default 0.95).}

\item{nsim}{Number of simulation replications.}

\item{seed}{Optional seed (the caller's RNG state is preserved).}

\item{vary}{Named list of scenario values for contextual inputs.
Parametric route: \code{baseline}, \code{signal}, \code{correlation} (scalar);
empirical route: \code{baseline}, \code{signal}. A length-2 element is expanded
to \code{grid_points} equally spaced values; longer vectors are used as-is.
Scenarios are the full factorial grid.}

\item{pin}{\code{"ape"} (re-invert per scenario) or \code{"coefficients"} (hold
coefficients, let the implied effect drift). Ignored in MDE mode,
which always re-solves.}

\item{mode}{\code{"power"} (default) reports power at the pinned effect per
scenario; \code{"mde"} reports the \strong{minimum detectable effect} per
scenario via \code{\link[=ape_mde]{ape_mde()}} -- the worst case is then the \emph{largest}
MDE, answering "what is the smallest effect this design finds even
under the least favorable contextual assumptions?".}

\item{power}{Target power for MDE mode (default 0.80; unused in power
mode).}

\item{grid_points}{Grid size used to expand length-2 \code{vary} elements.}

\item{nmax}{Run the n_max search in the worst scenario (default TRUE;
power mode only).}

\item{nmax_power}{Target power for the n_max search (default 0.90).}
}
\value{
A \code{powerape_robust} object: \code{scenarios} (inputs, implied effect,
power, MCSE), the worst scenario, marginal mean power per input, and
the n_max result.
}
\description{
Power computed from a single guess at the contextual inputs -- baseline
rate, nuisance signal, covariate dependence -- is fragile (Hancock &
Feng, 2025). \code{ape_robust()} re-runs the power analysis over a scenario
grid of those inputs and reports per-scenario power, the worst case, and
(optionally) \code{n_max}: the sample size that reaches the target power in
the worst scenario -- the insurance-premium n.
}
\details{
Two pinning modes (DESIGN.md section 3.3):
\itemize{
\item \code{pin = "ape"} (default): the inversion is re-run in every scenario, so
the true effect stays at the planning value and only the \emph{precision}
channel varies. Scenarios where the target is infeasible are reported
as such, not dropped silently.
\item \code{pin = "coefficients"}: the effect coefficients stay at their
base-case values while the contextual calibration is redone; the
implied true effect drifts and is reported per scenario. Claim-boundary
guards are relaxed here on purpose -- showing that a scenario collapses
power is the point.
}
}
\examples{
\donttest{
d <- ape_dgp(focal = pa_var("treat", "binary", p = 0.5), baseline = 0.30)
d <- set_ape(d, target = 0.10)
ape_robust(d, n = 700, claim = "detect",
           vary = list(baseline = c(0.20, 0.40)),
           nsim = 300, seed = 1, nmax = FALSE)
}
}
