% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve.R
\name{ape_curve}
\alias{ape_curve}
\alias{plot.powerape_curve}
\title{Power curve over a grid of sample sizes}
\usage{
ape_curve(
  dgp,
  n,
  claim = c("minimum", "detect", "equivalence"),
  sesoi = NULL,
  conf = 0.95,
  nsim = 1000,
  seed = NULL
)

\method{plot}{powerape_curve}(x, target_power = NULL, ...)
}
\arguments{
\item{dgp}{A \code{powerape_dgp} after \code{\link[=set_ape]{set_ape()}} or \code{\link[=set_aie]{set_aie()}}.}

\item{n}{Integer vector (length >= 2) of total sample sizes.}

\item{claim}{\code{"minimum"} (default), \code{"detect"}, or \code{"equivalence"}.}

\item{sesoi}{Smallest effect size of interest, in APE units. Required for
\code{"minimum"} and \code{"equivalence"}; optional for \code{"detect"} (if supplied,
the outcome table is still broken out against it).}

\item{conf}{CI level (default 0.95).}

\item{nsim}{Replications per grid point.}

\item{seed}{Optional; grid point i uses \code{seed + i - 1}.}

\item{x}{A \code{powerape_curve} object.}

\item{target_power}{Optional horizontal reference line; when the curve
crosses it, the crossing n is marked and annotated.}

\item{...}{Passed to the base plot call.}
}
\value{
A \code{powerape_curve} object with a \code{results} data frame
(\code{n}, \code{power}, \code{mcse}, \code{failed}).
}
\description{
Runs the power simulation at each element of \code{n} and collects the
results. Works for APE and AIE designs alike.
}
\examples{
\donttest{
d <- ape_dgp(focal = pa_var("treat", "binary", p = 0.5), baseline = 0.30)
d <- set_ape(d, target = 0.10)
cv <- ape_curve(d, n = c(300, 600, 900), claim = "detect", nsim = 300, seed = 1)
plot(cv, target_power = 0.8)
}
}
