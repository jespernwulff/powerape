% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{ape_dgp_panel}
\alias{ape_dgp_panel}
\title{Specify a panel DGP: correlated random effects probit}
\usage{
ape_dgp_panel(
  model = "probit",
  focal,
  moderator = NULL,
  covariates = list(),
  n_periods,
  p_periods = NULL,
  retention = NULL,
  rho = 0,
  cre_share = 0,
  correlation = NULL,
  baseline,
  signal = 0,
  n_int = 1e+05,
  seed_int = 20260814L
)
}
\arguments{
\item{model}{\code{"probit"} (the only panel link; see Details for why a
\code{"logit"} request is refused with a teaching error).}

\item{focal, covariates}{\code{\link[=pa_var]{pa_var()}} objects; use their \code{icc} argument to
set within-unit persistence (default 0; \code{icc = 1} = time-constant).}

\item{moderator}{Optional binary \code{\link[=pa_var]{pa_var()}} for panel AIE designs: the
index gains the moderator and a focal-by-moderator interaction, the
estimand becomes the AIE (pin it with \code{\link[=set_aie]{set_aie()}}), and the estimating
model gains the interaction's own Mundlak mean whenever the product is
time-varying. Currently binary focal x binary moderator only.}

\item{n_periods}{Number of periods per unit: a scalar for a balanced
panel, or an integer vector of possible lengths for an unbalanced
one (see Details). At least some units must have >= 2 periods.}

\item{p_periods}{Probabilities for a vector \code{n_periods} (same length;
equal weights if omitted). Not combined with \code{retention}.}

\item{retention}{Per-wave retention rate in (0, 1] for monotone
attrition from a scalar \code{n_periods} maximum; \code{retention = 1} is the
balanced panel. Not combined with \code{p_periods}.}

\item{rho}{Latent unit-effect share \code{Var(c) / (Var(c) + 1)} — the
xtprobit-style rho. 0 = no unobserved heterogeneity.}

\item{cre_share}{Fraction of \code{Var(c)} loaded (with equal standardized
weights) on the centered observed unit means of the time-varying
regressors; the remainder is the independent component \code{a_i}.
\code{cre_share = 0} gives a pure random-effects world; the estimator
includes Mundlak means either way.}

\item{correlation}{\code{NULL} (independence), a scalar exchangeable
correlation, or a full correlation matrix over
\verb{(focal, moderator, covariates)} -- Gaussian-copula dependence.}

\item{baseline}{Baseline outcome rate in (0, 1): the average probability
\code{P(Y = 1)} with the focal (and moderator, if any) set to their
reference values (0 for binary, the mean for continuous), covariates
at their population distribution. The intercept is calibrated to
reproduce it.}

\item{signal}{Nuisance-signal strength: the McKelvey-Zavoina-style latent
pseudo-R-squared of the nuisance index, i.e. \verb{Var(z'gamma) / (Var(z'gamma) + 1)}. Equal standardized weights, scaled to hit
\code{signal}. Ignored when \code{gamma} is supplied.}

\item{n_int, seed_int}{Size (in \strong{units}, each contributing its drawn
number of rows) and seed of the deterministic integration draw used
for calibration and inversion.}
}
\value{
An object of class \code{powerape_dgp} (route \code{"panel"}). Pin the
effect with \code{\link[=set_ape]{set_ape()}} (no moderator) or \code{\link[=set_aie]{set_aie()}} (with
moderator); the empirical/pilot routes are not yet available for
panel DGPs.
}
\description{
Panel power analysis under the most general applied panel binary-response
model: a correlated random effects (CRE) specification estimated by
pooled probit with Mundlak means and unit-clustered standard
errors (Wooldridge, 2010, ch. 15). Unobserved unit heterogeneity
\verb{c_i = xi'(centered observed unit means of time-varying regressors) + a_i}
may correlate with the regressors through the Mundlak part; the target
effect remains the ASF-based average partial effect, which the pooled
CRE estimator recovers even though its coefficients are attenuated.
}
\details{
The panel route is \strong{probit-only}. The exactness above rests on the
normal-normal convolution (a normal unit effect rescales the probit
index by \code{sqrt(1 + var_a)}); the logistic link has no such stability,
so a pooled CRE logit estimates only a quasi-maximum-likelihood
approximation of the ASF. Rather than price designs with an
approximate estimand, \code{ape_dgp_panel(model = "logit")} is refused with
an error explaining this. On the probability scale a probit world
calibrated to the same baseline and effect is practically
indistinguishable from the logit world it replaces.

Sample-size arguments of \code{\link[=ape_power]{ape_power()}}, \code{\link[=ape_curve]{ape_curve()}}, \code{\link[=ape_n]{ape_n()}}, and
\code{\link[=ape_robust]{ape_robust()}} refer to the \strong{number of units (clusters)} for panel
DGPs; each unit contributes its own number of observed periods.

\strong{Unbalanced panels} (Wooldridge, 2019) are specified by a
distribution over panel lengths: either give \code{n_periods} a vector of
possible lengths with probabilities \code{p_periods} (equal weights if
omitted), or give a scalar \code{n_periods} plus a \code{retention} rate, which
generates monotone attrition (each wave a unit remains with
probability \code{retention}; \code{P(T_i = t) = retention^(t-1) (1-retention)}
below the maximum). Selection is completely at random by
construction. Mundlak means are computed over each unit's observed
periods, and the estimating model gains period-count cohort
indicators, the workhorse specification of Wooldridge (2019); their
population coefficients are zero here, so their sample-size cost is
priced, exactly like the Mundlak insurance premium. \code{rho} remains
the latent unit-effect share averaged over the length mixture
(conditional on a unit's length it varies mildly with the number of
periods, through the sampling variance of the observed means).
}
\examples{
\donttest{
d <- ape_dgp_panel(
  focal = pa_var("treat", "binary", p = 0.5, icc = 1),  # unit-level treatment
  covariates = list(pa_var("size", "normal", icc = 0.6)),
  n_periods = 4, rho = 0.3, cre_share = 0.5,
  baseline = 0.30, signal = 0.10
)
d <- set_ape(d, target = 0.08)
ape_power(d, n = 150, claim = "detect", nsim = 300, seed = 1)  # n = units
}
}
