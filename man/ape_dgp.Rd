% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{ape_dgp}
\alias{ape_dgp}
\title{Specify the data-generating process for an APE/AIE power analysis}
\usage{
ape_dgp(
  model = c("probit", "logit"),
  focal,
  moderator = NULL,
  covariates = list(),
  correlation = NULL,
  baseline,
  signal = 0,
  gamma = NULL,
  n_int = 4e+05,
  seed_int = 20260812L
)
}
\arguments{
\item{model}{\code{"probit"} (default) or \code{"logit"}.}

\item{focal}{A \code{\link[=pa_var]{pa_var()}}, \code{"binary"} or \code{"normal"}. For a continuous
focal the APE is the average derivative, and reference = its mean.}

\item{moderator}{Optional \code{\link[=pa_var]{pa_var()}}. When present, the index includes
the moderator and a focal-by-moderator interaction, the estimand is
the average interaction effect (AIE, as in the \code{ginteff} package), and
the DGP must be pinned with \code{\link[=set_aie]{set_aie()}}.}

\item{covariates}{List of \code{\link[=pa_var]{pa_var()}} objects (may be empty).}

\item{correlation}{\code{NULL} (independence), a scalar exchangeable
correlation, or a full correlation matrix over
\verb{(focal, moderator, covariates)} -- Gaussian-copula dependence.}

\item{baseline}{Baseline outcome rate in (0, 1): the average probability
\code{P(Y = 1)} with the focal (and moderator, if any) set to their
reference values (0 for binary, the mean for continuous), covariates
at their population distribution. The intercept is calibrated to
reproduce it.}

\item{signal}{Nuisance-signal strength: the McKelvey-Zavoina-style latent
pseudo-R-squared of the nuisance index, i.e. \verb{Var(z'gamma) / (Var(z'gamma) + 1)}. Equal standardized weights, scaled to hit
\code{signal}. Ignored when \code{gamma} is supplied.}

\item{gamma}{Optional explicit nuisance coefficients (length =
\code{length(covariates)}); overrides \code{signal}.}

\item{n_int, seed_int}{Size and seed of the deterministic Monte Carlo
integration draw used for calibration and inversion.}
}
\value{
An object of class \code{powerape_dgp}. Pin the effect with
\code{\link[=set_ape]{set_ape()}} / \code{\link[=set_aie]{set_aie()}}, then use \code{\link[=ape_power]{ape_power()}}, \code{\link[=ape_curve]{ape_curve()}},
\code{\link[=ape_n]{ape_n()}}, or \code{\link[=ape_robust]{ape_robust()}}.
}
\description{
Describes the world the study will sample from: an index model
(probit/logit), a binary or continuous (normal) focal variable, an
optional moderator (for average-interaction-effect designs), covariates
with a Gaussian-copula dependence structure, and an outcome anchored on
the friendly scale -- the baseline outcome rate plus the strength of the
nuisance covariates. Effect coefficients are set later, in effect-size
units, by \code{\link[=set_ape]{set_ape()}} (no moderator) or \code{\link[=set_aie]{set_aie()}} (with moderator).
}
\examples{
d <- ape_dgp(
  model = "probit",
  focal = pa_var("treat", "binary", p = 0.5),
  covariates = list(pa_var("age", "normal", mean = 45, sd = 12)),
  baseline = 0.30, signal = 0.15
)
d
}
