% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv.R
\name{ape_dgp_iv}
\alias{ape_dgp_iv}
\title{Specify an IV DGP: endogenous focal, control-function probit}
\usage{
ape_dgp_iv(
  focal,
  moderator = NULL,
  covariates = list(),
  instruments,
  endogeneity,
  iv_strength,
  correlation = NULL,
  baseline,
  signal = 0,
  n_int = 1e+05,
  seed_int = 20260814L
)
}
\arguments{
\item{focal}{\code{\link[=pa_var]{pa_var()}}, \code{"normal"} (linear first stage) or \code{"binary"}
(probit first stage). The endogenous variable.}

\item{moderator}{Optional \strong{binary} \code{\link[=pa_var]{pa_var()}} for AIE designs.}

\item{covariates}{List of \code{\link[=pa_var]{pa_var()}} objects (may be empty).}

\item{instruments}{A \code{\link[=pa_var]{pa_var()}} or list of them: the excluded
instruments. Equal standardized first-stage weights.}

\item{endogeneity}{Correlation of the structural and first-stage errors
(u, v) -- the reason plain probit would be biased. In (-1, 1).}

\item{iv_strength}{First-stage strength: the share of the focal's
variance (latent variance for a binary focal) explained by the
instruments. In (0, 1); small values mean weak instruments.}

\item{correlation}{Dependence among the exogenous block (moderator,
covariates, instruments): scalar exchangeable correlation or a matrix.
The focal is generated by the first stage, not drawn.}

\item{baseline}{Baseline outcome rate in (0, 1): the average probability
\code{P(Y = 1)} with the focal (and moderator, if any) set to their
reference values (0 for binary, the mean for continuous), covariates
at their population distribution. The intercept is calibrated to
reproduce it.}

\item{signal}{Nuisance-signal strength: the McKelvey-Zavoina-style latent
pseudo-R-squared of the nuisance index, i.e. \verb{Var(z'gamma) / (Var(z'gamma) + 1)}. Equal standardized weights, scaled to hit
\code{signal}. Ignored when \code{gamma} is supplied.}

\item{n_int, seed_int}{Size and seed of the deterministic Monte Carlo
integration draw used for calibration and inversion.}
}
\value{
An object of class \code{powerape_dgp} (route \code{"iv"}). Pin the
effect with \code{\link[=set_ape]{set_ape()}} or, with a moderator, \code{\link[=set_aie]{set_aie()}}.
}
\description{
Power analysis when the focal variable is \strong{endogenous} and the study
will use instrumental variables via the control-function approach --
the estimator of Stata's \code{cfprobit} (replicated exactly, including its
stacked method-of-moments standard errors; no bootstrap). A continuous
focal gets a linear first stage, a binary focal a probit first stage;
the control function (first-stage residual or generalized residual)
joins the second-stage probit, whose coefficients margins-style ASF
averaging turns back into the APE/AIE.
}
\details{
The estimand is unchanged: the ASF-based APE of the focal (or the AIE
with a binary exogenous moderator). Because the structural error is
standard normal marginally, targets, feasibility, and truth evaluation
work exactly as in \code{\link[=ape_dgp]{ape_dgp()}}.

Two documented properties of the control-function estimator (validation
battery V13-V15): (1) for a \strong{binary} endogenous focal the generalized
residual is the standard Wooldridge approximation, with a small
negative bias (about -0.01 at \code{endogeneity = 0.4},
\code{iv_strength = 0.2}) that makes the directional detect claim
\emph{conservative} at the null (empirical size ~1.6\% against the nominal
2.5\%; the standard errors themselves are correctly calibrated);
(2) for a \strong{continuous} endogenous focal, the margins convention
(control functions held at observed values) evaluates the ASF over
the observed (focal, residual) pairs, which understates the
product-measure ASF slightly (about -0.005 on a 0.08 effect at
\code{endogeneity = 0.5}, \code{iv_strength = 0.3}) because the focal and its
residual are correlated by construction. powerape replicates the
field-standard (cfprobit/margins) convention exactly; a
product-measure ASF option is on the roadmap.

For AIE designs the estimating model includes the focal-by-moderator
term, the control function, and the control-function-by-moderator
interaction (Stata: \code{interact()} + \code{mainonly()}); the first stage
includes the moderator \strong{and the instrument-by-moderator interactions}
-- the general IV requirement for interaction models. Inference is
robust (stacked sandwich) throughout.
}
\examples{
\donttest{
d <- ape_dgp_iv(
  focal = pa_var("training", "binary", p = 0.4),
  covariates = list(pa_var("size", "normal")),
  instruments = pa_var("subsidy", "binary", p = 0.5),
  endogeneity = 0.4, iv_strength = 0.2,
  baseline = 0.30, signal = 0.10
)
d <- set_ape(d, target = 0.10)
ape_power(d, n = 1500, claim = "detect", nsim = 300, seed = 1)
}
}
